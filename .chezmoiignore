# 配置されるときの名前にする
# data は .chezmoi.toml.tmpl を参照

{{ if eq .name "runner" -}}
.ssh/id_ed25519.age
.ssh/authorized_keys
.ssh/id_rsa.age

{{ else if eq .name "dev" -}}
data

{{ else if eq .chezmoi.os "darwin" -}}
.config/fusuma
.config/fcitx5

{{ else if eq .chezmoi.os "linux" -}}
.config/Brewfile*
.config/karabiner
.ssh/id_rsa.age

{{ else -}}
.config/*
data/*
.devcontainer/*
cloud-init/*
*rc
*profile
*env

{{ end -}}

README.md
style.css
Makefile
install*

.env
.tool-versions
.mise.toml

.chezmoiexternal.*
key.txt.age
shhh.txt

.gitignore
.gitconfig
.github
.vscode
.devcontainer

cloud-init
