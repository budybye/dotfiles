emulate sh
. "${HOME}/.profile"
emulate zsh

typeset -U path PATH
path=(
  /opt/homebrew/bin(N-/)
  /opt/homebrew/sbin(N-/)
  /opt/X11/bin(N-/)
  ${HOME}/.console-ninja/.bin(N-/)
  ${HOME}/.local/share/sheldon/repos/github.com/*/*/bin(N-/)
  ${PATH}
  ${HOME}/.local/bin(N-/)
  /usr/local/bin(N-/)
  /usr/bin
  /bin
  /usr/sbin
  /sbin
  /snap/bin(N-/)
)

. "${HOME}/.zshrc"
