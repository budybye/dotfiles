# .zshenv は全てのシェルで読み込まれる
typeset -U PATH path
path=(
  /usr/local/bin(N-/)
  /usr/bin(N-/)
  /bin(N-/)
  ${PATH}
)
# スクリプトでも必要な最小限の環境変数のみをzshenvに記述

PROMPT='%F{green}%n@%m%f:%F{blue}% ~ %f%F{yellow}$%f '
